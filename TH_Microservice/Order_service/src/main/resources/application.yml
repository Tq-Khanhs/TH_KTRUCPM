server:
  port: 8082
spring:
  application:
    name: Order_Service
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        exclude: '*'
logging:
  level:
    root: info
    web: info
